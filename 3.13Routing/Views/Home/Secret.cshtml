@using _3._13Routing
@using _3._13Routing.Models
@using LoginUser
@model ViewModel

@*id
    title
    userid
    url
    date
    points*@
<div class="container">
    <h2>Welcome @Model.User.UserName</h2>
    <a href="/home/index">Home</a>
</div>

<table class="table table-bordered table-striped table-hover" id="ptable" name="table">
    <tr>
        <th>Number</th>
        <th>Upvote Button</th>
        <th>Details</th>
    </tr>

    <tr>
        @foreach (Link l in Model.linksByUser)
        {
            <td>Number</td>
            <td>@l.Uploads</td>
            <td>@l.Url</td>
        }

    </tr>


    @*<tr>
        @foreach (Link link in Model.linksByUser)
        {


            <td>@(ViewBag.Counter)</td>
            <td><a href="/home/upvote?postId=@Model.User.Id">&#9650;</a></td>
            <td>

                @*if want to show host name and populate list by host name
                    <a href="/home/hostname?hostName=@Model.User.URLHostName">*@

    @*<span style="display:block">
                        <a href="@link.Url">@link.Url</a>
                    </span>
                    <span>@link.Uploads.Count() points by <a href="/home/users?userId=@Model.User.Id">@Model.User.UserName</a> <time class="timeago" datetime="@Model.Date">@Model.Date</time></span>
                </td>

            }
        </tr>*@
</table>




<div class="container">

    <button class="btn btn-primary btn-lg" id="Link-button" data-target="#myModal-Link" data-toggle="modal">Add a Link</button>



</div>

<div id="myModal-Link" class="modal fade modal input" role="dialog">
    <div class="modal-dialog">
        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Add a Link and enhance our Programming Site</h4>
            </div>
            <form method="POST" action="/home/Post" enctype="multipart/form-data">


                Title: <input type="text" name="Title" class="form-control" id="Title"><br>
                UserId should show <input type="number" name="UserId" class="form-control" id="UserId" value="@Model.UserId"><br>
                Url: <input type="text" name="Url" class="form-control" id="Url" placeholder="Paste your Url"><br>
                Date:  <input type="date" name="Date" class="form-control" id="Date"><br>
                @*Points: <input type="number" name="" class="form-control" id="AddProductImagOne" placeholder="Image">*@
                @*points may be null cuz when you add it in you not want to set it*@
                <br />

                <button class="btn btn-primary" data-dismiss id="Submit">Submit</button>

            </form>

            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>

    </div>
</div>

@section scripts{
    <script src="~/Scripts/Scripts.js"></script>
}
